<UserControl x:Class="TNovUtils.Checklist.UI.SummaryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TNovCommon;component/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="CompactCheckCardStyle" TargetType="Border" BasedOn="{StaticResource ChangeCardStyle}">
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Margin" Value="0,0,0,4"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="80"/>
            <RowDefinition Height="*" MinHeight="80"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Свод по проверкам"
                   Style="{StaticResource PanelHeaderStyle}"
                   HorizontalAlignment="Center"
                   FontSize="14"
                   Margin="0,0,0,8"/>

        <Button Grid.Row="1"
                Content="Обновить все проверки"
                Command="{Binding RunAllCommand}"
                Style="{StaticResource OutlinedSecondaryButtonStyle}"
                HorizontalAlignment="Stretch"
                Padding="14,8"
                Margin="0,0,0,8"/>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,0,0,8">
            <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CompactCheckCardStyle}" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding OpenCommand}"/>
                            </Border.InputBindings>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="8" Height="8"
                                         Fill="{Binding StatusBrush}"
                                         Margin="0,0,8,0"
                                         VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}"
                                               Style="{StaticResource JournalModuleStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayDate}"
                                                   Style="{StaticResource JournalDateStyle}"
                                                   Margin="0,1,0,0"/>
                                        <TextBlock Text="{Binding LastRunBy}"
                                                   Style="{StaticResource JournalDateStyle}"
                                                   Margin="8,1,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Grid.Column="2"
                                           Text="{Binding StatusText}"
                                           Foreground="{Binding StatusBrush}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ContentControl x:Name="DesignerHost"
                      Grid.Row="3"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
