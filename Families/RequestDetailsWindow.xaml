<Window x:Class="TNovUtils.RequestDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали заявки" Height="500" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TNovCommon;component/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" ResizeBorderThickness="5"
                      GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>
    <Border CornerRadius="12" Background="White" BorderBrush="#FF008E43"
            BorderThickness="1">
        <Grid>
            <!-- Кастомный заголовок -->
            <Border Height="30" CornerRadius="12 12 0 0" Background="#FF008E43"
                    VerticalAlignment="Top" MouseDown="TitleBar_MouseDown">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource RoundButtonStyle}" Content="✕"
                            Click="CloseButton_Click"
                            Width="20" Height="20" Margin="5,0"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Style="{StaticResource RoundButtonStyle2}" 
Click="HelpButton_Click" Content="?"
HorizontalAlignment="Center" VerticalAlignment="Center"
Width="20" Height="20" Margin="0"
WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <TextBlock Text="Детали заявки" Foreground="White"
                               VerticalAlignment="Center" Margin="10,0"/>
                </StackPanel>
            </Border>

            <!-- Основное содержимое -->
            <Border Margin="0,30,0,0">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"
                               Text="Детали заявки"/>

                    <!-- Таблица свойств -->
                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="ID:" Grid.Row="0" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbId" Grid.Row="0" Grid.Column="1"/>

                        <TextBlock Text="Дата создания:" Grid.Row="1" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbDate" Grid.Row="1" Grid.Column="1"/>

                        <TextBlock Text="Заявитель:" Grid.Row="2" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbRequester" Grid.Row="2" Grid.Column="1"/>

                        <TextBlock Text="Ответственный:" Grid.Row="3" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbAssignee" Grid.Row="3" Grid.Column="1"/>

                        <TextBlock Text="Объект:" Grid.Row="4" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbProject" Grid.Row="4" Grid.Column="1"/>

                        <TextBlock Text="Срок:" Grid.Row="5" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Name="tbDeadline" Grid.Row="5" Grid.Column="1"/>
                    </Grid>

                    <!-- Статус -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Статус:" FontWeight="Bold" Margin="0,0,5,0"/>
                        <TextBlock Name="tbStatus" />
                    </StackPanel>

                    <!-- Описание -->
                    <GroupBox Grid.Row="3" Header="Описание" Margin="0,0,0,8">
                        <TextBox Name="tbDescription" IsReadOnly="True" TextWrapping="Wrap"
                                 MinHeight="60" VerticalScrollBarVisibility="Auto" Background="White"/>
                    </GroupBox>

                    <!-- Фото -->
                    <GroupBox Grid.Row="4" Header="Фото / образец" Margin="0,0,0,8">
                        <Border BorderBrush="Gray" BorderThickness="1" Background="#F0F0F0" MinHeight="120">
                            <Grid>
                                <Image Name="imgPhoto" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Name="tbNoPhoto" Text="Фото отсутствует"
                                           VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray"/>
                            </Grid>
                        </Border>
                    </GroupBox>

                    <Button Style="{StaticResource CustomButtonStyle}" Grid.Row="5" Content="Закрыть" Width="80" Height="25" HorizontalAlignment="Right"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>