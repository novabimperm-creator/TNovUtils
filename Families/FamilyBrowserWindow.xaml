<Window x:Class="TNovUtils.FamilyBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Библиотека семейств" Height="500" Width="850"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TNovCommon;component/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" ResizeBorderThickness="5"
                      GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>
    <Border CornerRadius="12" Background="White" BorderBrush="#FF008E43"
            BorderThickness="1">
        <Grid>
            <!-- Заголовок -->
            <Border Height="30" CornerRadius="12 12 0 0" Background="#FF008E43"
                    VerticalAlignment="Top" MouseDown="TitleBar_MouseDown">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource RoundButtonStyle}" Content="✕"
                            Click="CloseButton_Click"
                            Width="20" Height="20" Margin="5,0"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Style="{StaticResource RoundButtonStyle2}" 
                            Click="HelpButton_Click" Content="?"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Width="20" Height="20" Margin="0"
                            WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <TextBlock Text="Библиотека семейств" Foreground="White"
                               VerticalAlignment="Center" Margin="10,0"/>
                </StackPanel>
            </Border>

            <!-- Содержимое -->
            <Border Margin="0,30,0,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Панель поиска и фильтрации -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Name="SearchBox" Width="200" Margin="0,0,15,0"
                                 TextChanged="OnSearchTextChanged"/>
                        <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox Name="CategoryFilter" Width="150"
                                  SelectionChanged="OnCategoryChanged"/>
                        <Button Style="{StaticResource CustomButtonStyle}" Content="Сбросить" 
                                Margin="15,0,5,0" Click="ResetFilters_Click" Width="80" Height="25"/>
                        <Button Style="{StaticResource CustomButtonStyle}" Content="Обновить кэш семейств" 
                                Click="RefreshCache_Click" Width="160" Height="25" Margin="10 0 0 0"/>
                    </StackPanel>

                    <!-- DataGrid с семействами -->
                    <DataGrid Grid.Row="1" Grid.RowSpan="2" Name="FamiliesGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True" SelectionMode="Single"
                              SelectionUnit="FullRow" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Версия" Binding="{Binding Version}" Width="80"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Дата изменения"
                                                Binding="{Binding LastModified, StringFormat=dd.MM.yyyy HH:mm}"
                                                Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Кнопки -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal"
                                HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Style="{StaticResource CustomButtonStyle}" Name="CancelBtn" Content="Отмена" 
                                Width="80" Height="25" Click="CloseButton_Click"/>
                        <Button Style="{StaticResource CustomButtonStyle}" Name="LoadBtn" Content="Загрузить семейство"
                                Width="150" Height="25" Margin="10,0,0,0"
                                Click="LoadBtn_Click"/>
                    </StackPanel>
                    <Button Style="{StaticResource CustomButtonStyle}" Name="RequestBtn" Content="Заявка на семейство"
                            Width="150" Height="25" Margin="0,10,670,0"
                            Click="RequestBtn_Click" Grid.Row="3"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>