<Window x:Class="TNovUtils.Issues.UI.UserPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор ответственных" Height="520" Width="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TNovCommon;component/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" x:Name="SearchBox" Padding="4" Margin="0,0,0,6"
                 TextChanged="SearchBox_TextChanged"
                 ToolTip="Поиск по ФИО / должности / логину"/>
        <TextBlock Grid.Row="0" IsHitTestVisible="False" Foreground="#999" Margin="6,0,0,0"
                   VerticalAlignment="Center" Text="Поиск сотрудника…">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <TextBlock Grid.Row="1" x:Name="StatusText" Foreground="#666" Margin="0,0,0,6" Text="Загрузка…"/>

        <ListBox Grid.Row="2" x:Name="UsersList" HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="2,3" Padding="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding User.DisplayName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding User.Subtitle}" Foreground="#888" FontSize="11"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="CountText" VerticalAlignment="Center" Foreground="#666" Margin="0,0,12,0"/>
            <Button Content="Отмена" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Готово" Width="110" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
